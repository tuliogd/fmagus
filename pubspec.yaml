name: fmagus
description: Dart package to automate the boring work, like creating assets, folder structures, importing packages etc for Flutter via CLI.
version: 1.0.6
homepage: https://github.com/tuliogd/fmagus

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  args: ^2.4.1
  process_run: ^0.13.0
  image: ^4.0.16

dev_dependencies:
  lints: ^2.0.1
